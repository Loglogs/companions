import React from 'react';
import { createRoot } from 'react-dom/client';
import InstallApp from './InstallApp';

createRoot(document.getElementById('root')!).render(<React.StrictMode><InstallApp /></React.StrictMode>);
